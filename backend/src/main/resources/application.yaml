spring:
  application:
    name: tourplaner
  datasource:
    url: jdbc:postgresql://192.168.1.113:5432/tourDB
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
